# Purpose: Run the FastAPI signal-notes app on 127.0.0.1:8400. nginx
#          (Phase 9) proxies /api/notes to this upstream. Data lives on
#          tmpfs (/run/signal-notes) — a reboot wipes the board, by spec.
# Unit:    signal-notes.service
# Phase:   9 (9A — local board; mesh propagation lands in 9.2)
#
# The tmpfs mount is created by systemd via RuntimeDirectory= so we
# don't need a separate fstab line. The dir lives only as long as the
# unit is active; that gives us "reboot wipes" semantics for free.

[Unit]
Description=SIGNAL ephemeral notes board (FastAPI)
After=network.target signal-ap.service
PartOf=signal-ap.service

[Service]
Type=simple
DynamicUser=yes

# RuntimeDirectory creates /run/signal-notes owned by the dynamic user
# and removes it when the unit stops. StateDirectory would persist; we
# do not want that here — ephemeral is the spec.
RuntimeDirectory=signal-notes
RuntimeDirectoryMode=0750

Environment=PYTHONPATH=/opt/signal
Environment=PYTHONDONTWRITEBYTECODE=1
Environment=SIGNAL_NOTES_DB=/run/signal-notes/notes.sqlite
Environment=SIGNAL_NOTES_TOKEN_FILE=/etc/signal/notes-owner-token
WorkingDirectory=/opt/signal

ExecStart=/usr/bin/python3 -m uvicorn notes.signal_notes.main:app \
    --host 127.0.0.1 \
    --port 8400 \
    --log-level warning \
    --no-access-log

Restart=on-failure
RestartSec=3
TimeoutStopSec=10

MemoryMax=96M
TasksMax=64

NoNewPrivileges=yes
PrivateTmp=yes
PrivateDevices=yes
ProtectSystem=strict
ProtectHome=yes
ProtectKernelTunables=yes
ProtectKernelModules=yes
ProtectKernelLogs=yes
ProtectControlGroups=yes
ProtectClock=yes
RestrictNamespaces=yes
RestrictRealtime=yes
RestrictSUIDSGID=yes
LockPersonality=yes
MemoryDenyWriteExecute=yes
SystemCallArchitectures=native
SystemCallFilter=@system-service
SystemCallFilter=~@privileged @resources
RestrictAddressFamilies=AF_INET AF_INET6 AF_UNIX
ReadOnlyPaths=/opt/signal /etc/signal

[Install]
WantedBy=multi-user.target
