# Purpose: Run the FastAPI signal-assist app on 127.0.0.1:8200. nginx
#          (Phase 6) proxies /api/ask to this upstream. The service wraps
#          retrieval with the safety classifier, prompt scaffold, and a
#          llama.cpp HTTP client. It is the "Ask" tile's backend.
# Unit:    signal-assist.service
# Phase:   6
#
# Dependency chain:
#   signal-llama.service     (the model server; ships separately, fetch_models.sh)
#   signal-retrieve.service  (hybrid retrieval)
#   signal-assist.service    (this — composes the other two)
#
# Stays inactive when no Qwen weights are present.

[Unit]
Description=SIGNAL retrieval-grounded answer API (FastAPI)
After=network.target signal-retrieve.service signal-llama.service
Wants=signal-retrieve.service signal-llama.service
PartOf=signal-ap.service
ConditionPathExists=/var/lib/signal/models/qwen2.5-1.5b-instruct-q4_k_m.gguf

[Service]
Type=simple
DynamicUser=yes

Environment=PYTHONPATH=/opt/signal
Environment=PYTHONDONTWRITEBYTECODE=1
WorkingDirectory=/opt/signal

ExecStart=/usr/bin/python3 -m uvicorn assistant.signal_assist.main:app \
    --host 127.0.0.1 \
    --port 8200 \
    --log-level warning \
    --no-access-log

Restart=on-failure
RestartSec=5
TimeoutStopSec=15

# Stays light: this service shells the heavy lifting to signal-llama.
MemoryMax=192M
TasksMax=64

NoNewPrivileges=yes
PrivateTmp=yes
PrivateDevices=yes
ProtectSystem=strict
ProtectHome=yes
ProtectKernelTunables=yes
ProtectKernelModules=yes
ProtectKernelLogs=yes
ProtectControlGroups=yes
ProtectClock=yes
RestrictNamespaces=yes
RestrictRealtime=yes
RestrictSUIDSGID=yes
LockPersonality=yes
MemoryDenyWriteExecute=yes
SystemCallArchitectures=native
SystemCallFilter=@system-service
SystemCallFilter=~@privileged @resources
RestrictAddressFamilies=AF_INET AF_INET6 AF_UNIX
ReadOnlyPaths=/var/lib/signal /opt/signal

[Install]
WantedBy=multi-user.target
